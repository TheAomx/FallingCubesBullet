#include <random>

#ifndef UTILS_H
#define	UTILS_H

class Utils {
public:
	static double getRandNumber(double min, double max);
	static float getRandNumber(float min, float max);
private:

};

#endif	/* UTILS_H */

